datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User{
    id String @default(nanoid()) @id @unique
    username String @unique
    password String 
}

